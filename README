A RESTful API with FastApi web framework that allows consumers to manage vessels:create, update,retrieve a list of vessels from the server.
An additinal feature is to make above operations by date and destination of the vessel. 
For the DataBase I used SQLite.

RUN the app:


Option 1:
*use Docker
docker build -t NAME .
docker run -p 80:80 NAME


Option 2:
install requirements;
uvicorn main:app --reload

You can use Interactive API docs (/docs) or alternative automatic documentation for UI(/redoc)